# Core ML
torch>=2.0.1
transformers>=4.40.0
accelerate>=0.30.0

# Story generation
spacy>=3.7.0
# After install: python -m spacy download en_core_web_sm

# Optional 4-bit quantization for local LLM (Linux/CUDA; gracefully skipped otherwise)
bitsandbytes>=0.43.0 ; platform_system != "Windows"

# CPU-friendly local backend via GGUF (works on Windows, no CUDA needed).
# Used when STORY_BACKEND=llama_cpp.
llama-cpp-python>=0.2.90

# Optional API backend
anthropic>=0.40.0

# Speech recognition
faster-whisper>=1.0.0

# Text-to-speech: chain kokoro (primary) -> piper -> pyttsx3 (fallback).
# kokoro additionally requires the system package 'espeak-ng' for English G2P.
kokoro>=0.7.0
piper-tts>=1.2.0
pyttsx3==2.90

# Audio I/O
sounddevice==0.4.5
soundfile==0.12.1
numpy>=1.24.3

# Config
python-dotenv==1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
loguru>=0.7.0

# Dev / test
pytest>=7.3.1
pytest-cov>=4.1.0
black>=23.3.0
flake8>=6.0.0
mypy>=1.0.1
